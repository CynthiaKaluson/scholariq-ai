# Scholariq-AI — Copilot Instructions (AUTHORITATIVE)

You are working on an academic writing backend called **Scholariq-AI**.
This file defines NON-NEGOTIABLE architectural rules.
Do NOT assume. Do NOT invent features. Do NOT duplicate logic.

---

## 1. SINGLE SOURCE OF TRUTH — ENUMS (MANDATORY)

- `CitationStyle` MUST exist ONLY in:
  - `app/models/schemas.py`

- NEVER redefine `CitationStyle` anywhere else.
- All services (including `citation_validator.py`, `gemini.py`) MUST import:
  ```python
  from app.models.schemas import CitationStyle
